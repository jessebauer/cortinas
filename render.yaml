services:
  - type: web
    name: cortina-app
    runtime: node
    buildCommand: npm install && npm rebuild better-sqlite3 && npm run build
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
